package main

import "go-api/app"

func main() {
	app.StartApp()
}

/*
	notas, err, err1 := items.ErrorTesting(-1)
	if err != nil{
		fmt.Println(err)
		return
	}
	if err1 != nil {
		fmt.Println(err1)
		return
	}
	println(notas)

*/
